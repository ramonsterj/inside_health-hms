# Roles and Functionality Matrix

## Scope

This matrix describes the shipped system roles in the codebase today.

It is based on:

- backend authorization checks in the API controllers and services
- default role grants in `api/src/main/resources/db/seed/R__seed_01_reset_and_base.sql`
- versioned role and permission migrations in `api/src/main/resources/db/migration`
- frontend route and menu gating in `web/src/router/index.ts` and `web/src/layout/AppMenu.vue`

Custom roles can be created in the platform, so this document only covers the built-in roles and the default grants that ship with the project.

## Legend

| Value | Meaning |
| --- | --- |
| Full | Create, update, delete, and manage the feature area |
| Work | Day-to-day operational use, but not full system administration |
| Read | View or use existing records only |
| Scoped | Access is limited by extra business rules beyond the raw permission |
| Self | Own profile/account only |
| None | No shipped access |
| * | Has `user:read` at the API level, but the frontend hides the screen behind `requiresAdmin` |
| ** | Granted by `V084__seed_treasury_permissions.sql` but lost after reseed — see Mismatches section |

## Matrix

`NURSE` is the **graduate nurse** (enfermero graduado — full nursing scope). `AUXILIARY_NURSE` (enfermero auxiliar, V117) is a restricted subset that records vital signs and nursing notes only.

| Functionality | ADMIN | ADMINISTRATIVE_STAFF | DOCTOR | RESIDENT_DOCTOR | PSYCHOLOGIST | NURSE | AUXILIARY_NURSE | CHIEF_NURSE | USER | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Own profile, password, locale | Self | Self | Self | Self | Self | Self | Self | Self | Self | `/api/users/me` endpoints are available to any authenticated user. |
| User administration | Full | None* | None* | None* | None | None* | None | None* | None* | Frontend user-management screen is admin-only. AUXILIARY_NURSE lacks even `user:read`. |
| Role administration | Full | None | None | None | None | None | None | None | None | Frontend role-management screen is admin-only. |
| Audit logs | Read | None | None | None | None | None | None | None | None | Frontend audit-log screen is admin-only. |
| Patients | Full | Work | Scoped | Work | Scoped | Read | Read | Work | None | Staff can create/update. Standalone `DOCTOR` is scoped to assigned patients (treating or consulting physician) on patient list/detail. `RESIDENT_DOCTOR` sees every patient because residents run admissions hospital-wide (V114/V115). Psychologists are limited to patients with active admissions; psychologists also have `patient:update`. AUXILIARY_NURSE has `patient:read` only. |
| Patient ID documents | Full | Work | None | None | None | None | None | None | None | Staff can upload, view, and delete because delete reuses `patient:upload-id`. |
| Admissions | Full | Work | Scoped | Work | Scoped | Read | Read | Work | None | Staff and chief nurse can update and discharge. Standalone `DOCTOR` admission lists are scoped to treating or consulting physician. `RESIDENT_DOCTOR` sees every admission and additionally holds `admission:create` (V114); the create endpoint auto-binds the resident slot to the authenticated user. **Only a `RESIDENT_DOCTOR` may admit** — `ADMIN` is the sole exception and admits by explicitly picking the resident (`residentId`), since an admin is not a resident (V119 revokes `RESIDENT_DOCTOR` from the seeded `admin`). `ADMINISTRATIVE_STAFF` holds `admission:create` but is rejected with 400 `error.admission.resident.role.required` because it is neither admin nor resident. Psychologist lists/search/detail are active-only. AUXILIARY_NURSE holds `admission:read` only (V117) — same as NURSE; it does **not** hold `admission:update`, so it cannot discharge, edit admission metadata, or change consulting physicians (those endpoints are all gated by `admission:update`). |
| Admission PDF export | Full | Work | None | None | None | None | None | None | None | `admission:export-pdf` is granted only to ADMIN and ADMINISTRATIVE_STAFF by V099. The export is generated synchronously, streamed once, and audited with `ADMISSION_EXPORT`. |
| Admission consent document | Full | Work | Read | Read | Read | Read | None | Read | None | Download endpoint is permission-based only. AUXILIARY_NURSE has no `admission:view-consent`. |
| Admission documents | Full | Work | Read | Read | Read | Read | None | Read | None | Staff can upload; clinical roles can view/download. AUXILIARY_NURSE has no `admission:view-documents`. |
| Clinical history | Full | None | Work | Work | None | Read | Read | Read | None | Only admin has default update permission. Doctors / residents can create/read. Nurses, auxiliary nurses, and chief nurses are read-only. |
| Progress notes | Full | None | Work | Work | None | Work | Read | Work | None | Append-only for non-admins. Doctor / resident / nurse / chief nurse can `create` and `read`; only ADMIN holds `progress-note:update`. CHIEF_NURSE gains `progress-note:create` in V096; the legacy `progress-note:update` grant for CHIEF_NURSE in the seed file is revoked in V096. **AUXILIARY_NURSE is read-only** — it holds `progress-note:read` but not `progress-note:create`. Discharge protection blocks writes for everyone, including ADMIN. |
| Medical orders | Full | Read | Work | Work | Scoped *(planned, v1.6)* | Read | Read | Read | None | Doctors / residents can create and discontinue. Only admin has default `medical-order:update`. Initial state is category-driven: directive categories (diet / mobility / etc.) start in `ACTIVA`; auth-required categories (medications / labs / referrals / psychometric) start in `SOLICITADO`. Administrative staff gains read access in order to authorize. **Planned (v1.6 spec):** PSYCHOLOGIST gains `medical-order:read` but the service-layer scopes both list and single-order reads to `category = PRUEBAS_PSICOMETRICAS`. |
| Medical-order authorization | Full | Work | None | None | None | None | None | None | None | `medical-order:authorize` lets the role transition `SOLICITADO → AUTORIZADO` or `NO_AUTORIZADO`. Only valid for auth-required categories. Granted to admin and administrative staff by default. |
| Medical-order emergency authorize | Full | None | Work | Work | None | None | None | None | None | `medical-order:emergency-authorize` lets a doctor / resident self-authorize an order without admin staff (after-hours, patient in crisis, family unreachable). Requires a structured reason enum + optional note (required when reason is OTHER). Stamps `emergency_*` audit fields and triggers billing the same as normal authorize. Granted to admin, doctor, and resident by default. |
| Medical-order mark in progress | Full | None | Work | Work | Scoped *(planned, see [nursing-roles-split.md](features/nursing-roles-split.md) + [medical-psychiatric-record.md](features/medical-psychiatric-record.md) v1.6)* | Work | None | Work | None | `medical-order:mark-in-progress` transitions `AUTORIZADO → EN_PROCESO` for labs / referrals / psychometric tests (sample taken / patient referred / test administered). Owns the nursing worklist. After this transition, discontinue is no longer allowed — the action has been initiated externally. The `RESULTADOS_RECIBIDOS` terminal state is reached implicitly when a result document is uploaded. Granted to admin, nurse, doctor, resident, and chief nurse by default. **AUXILIARY_NURSE is excluded** (no grant) and `MedicalOrderService.markInProgress` additionally guards with a 403 `error.nursing.auxiliary.denied`. **Planned (v1.6 spec):** PSYCHOLOGIST gains the permission but the service rejects the call unless `category = PRUEBAS_PSICOMETRICAS`. |
| Medical-order documents | Full | None | Work | Work | Scoped *(planned, v1.6)* | Work | Read | Read | None | Doctor / resident / nurse can upload. Uploading on `AUTORIZADO` or `EN_PROCESO` auto-transitions to `RESULTADOS_RECIBIDOS`. Chief nurse can read/download through `medical-order:read` but cannot upload by default. There is no separate download permission; download is gated by `medical-order:read`. **AUXILIARY_NURSE can read/download but not upload** (no `medical-order:upload-document`); `MedicalOrderDocumentService.uploadDocument` additionally guards with a 403. **Planned (v1.6 spec):** PSYCHOLOGIST gains upload-document but the service rejects the call unless `category = PRUEBAS_PSICOMETRICAS`. |
| Medical orders by state (cross-admission dashboard) | Full | Read | Read | Read | Scoped *(planned, v1.6)* | Read | Read | Read | None | Top-level screen at `/medical-orders`. Visible to any role with `medical-order:read`. Default filter selects the action-needed buckets (`SOLICITADO`, `AUTORIZADO`, `EN_PROCESO`). Action buttons inside the screen are still gated by the underlying permission (authorize / emergency-authorize / mark-in-progress / discontinue / upload-document), so AUXILIARY_NURSE sees the list but no action buttons. **Planned (v1.6 spec):** PSYCHOLOGIST gains `medical-order:read` so the route and endpoint are reachable, but the listing is service-layer scoped to `category = PRUEBAS_PSICOMETRICAS` — PSYCHOLOGIST only sees the psychometric orders they are allowed to act on. |
| Nursing notes | Full | None | Work | Work | None | Work | Work | Work | None | Append-only for non-admins. Doctor / resident / nurse / auxiliary nurse / chief nurse can `create` and `read`; only ADMIN holds `nursing-note:update` after V096. Discharge protection blocks writes for everyone, including ADMIN. |
| Vital signs | Full | None | Work | Work | None | Work | Work | Work | None | Append-only for non-admins. Doctor / resident / nurse / auxiliary nurse / chief nurse can `create` and `read`; only ADMIN holds `vital-sign:update` after V097. Discharge protection blocks writes for everyone, including ADMIN. |
| Medication administration | Full | None | Read | Read | None | Work | Read | Work | None | Create is limited to medication orders on active admissions and requires an inventory-linked order for `GIVEN`. **AUXILIARY_NURSE can read the MAR but not administer** (no `medication-administration:create`); `MedicationAdministrationService.createAdministration` additionally guards with a 403. |
| Psychotherapy activities | Full | None | Read | Read | Work | Read | None | Read | None | Create also requires the actual `PSYCHOLOGIST` role and a hospitalization admission. Delete is effectively admin-only in shipped roles. AUXILIARY_NURSE has no psychotherapy access. |
| Psychotherapy categories | Full | None | Read | Read | Read | Read | None | Read | None | Category management is admin-only by default. |
| Billing charges and balances | Full | Read | Read | Read | Read | Read | None | Read | None | Manual charges and adjustments are admin-only in shipped roles. Staff and psychologist read access comes from the seed file, not the versioned billing migration. AUXILIARY_NURSE has no `billing:read`. |
| Invoices | Full | Read | None | None | None | None | None | None | None | Invoice generation is admin-only in shipped roles. Staff invoice-read access comes from the seed file, not the versioned billing migration. |
| Triage codes | Full | Read | Read | Read | Read | Read | None | Read | None | Management is admin-only by default. AUXILIARY_NURSE has no `triage-code:read`. |
| Rooms | Full | Read | Read | Read | Read | Read | None | Read | None | Management is admin-only by default. AUXILIARY_NURSE has no `room:read`, but does hold `room:occupancy-view` (bed-occupancy screen). |
| Document types | Full | Read | None | None | None | None | None | None | None | Routes and views exist, but there is no sidebar entry for document types. |
| Inventory categories, items, and stock movements | Full | None | None | None | None | None | None | None | None | Inventory movement records can still be created indirectly by medication administration flows. |
| Treasury operations | Full | Full** | None | None | None | None | None | None | None | Bank accounts, expenses, income, employees, payroll, and doctor fees. |
| Treasury reports | Full | Full** | None | None | None | None | None | None | None | Dashboard plus monthly, upcoming, bank summary, compensation, indemnizacion, and reconciliation summary reports. |
| Treasury reconciliation | Full | Full** | None | None | None | None | None | None | None | Bank statements and reconciliation workflow. |

## Important Enforcement Notes

- The frontend treats `ADMIN` as a superuser. `web/src/stores/auth.ts` returns `true` for any permission check when the user has the `ADMIN` role.
- `/users`, `/roles`, and `/audit-logs` are gated in the frontend by `requiresAdmin`, not just by raw permissions.
- Nurses, auxiliary nurses, and chief nurses are redirected from `/dashboard` to `/nursing-kardex`.
- `AUXILIARY_NURSE` (V117) is the auxiliary nurse — `NURSE` is now the graduate nurse. The auxiliary holds nursing-note and vital-sign create/read plus read-only context, but the three restricted write actions (administer medication, mark order in progress, upload result document) are blocked by both the absence of the underlying permission and a service-layer guard (`error.nursing.auxiliary.denied`) that fires when the caller's only nursing-or-better role is `AUXILIARY_NURSE`. A user stacked with `NURSE`/`CHIEF_NURSE`/`DOCTOR`/`ADMIN` passes the guard. Quick-action buttons on the kardex / medical-order cards are already hidden for the auxiliary because they are gated by the underlying permissions. See [features/nursing-roles-split.md](features/nursing-roles-split.md).
- The nursing kardex itself is keyed off `admission:read`, so staff, doctors, psychologists, and admin can also reach it if they navigate there.
- Doctor scoping is partial, not universal. Patient list/detail and admission list are scoped to the treating physician **or** consulting physicians, but `AdmissionService.getAdmission()` does not enforce physician scoping on direct admission detail fetches.
- `RESIDENT_DOCTOR` (V114) is permission-equivalent to `DOCTOR` plus `admission:create`. Scoping behaves differently: every patient and admission listing returns the full set for residents — the standalone `DOCTOR` scope to assigned patients does **not** apply when the user also holds `RESIDENT_DOCTOR`. The admission detail view shows both treating physician and resident. The admission create flow auto-binds the resident slot to the authenticated user and rejects with `error.admission.resident.role.required` when the caller lacks `RESIDENT_DOCTOR`. **`ADMIN` is the only exception**: an admin is not a resident, so `AdmissionService.resolveResident()` requires the admin to pass a `residentId` pointing to a `RESIDENT_DOCTOR` (400 `error.admission.resident.required` if omitted, 400 `error.admission.resident.invalid.role` if the picked user lacks the role). V119 revokes `RESIDENT_DOCTOR` from the seeded `admin`; the admission form shows admins a resident picker (`GET /api/v1/admissions/residents`).
- Psychologist scoping is also partial. The code enforces active-only behavior for patient list/detail, admission list/detail, patient search, patient summary, and consulting physician list, but the admission document and consent download endpoints are only permission-gated.
- Nursing notes, progress notes, and vital signs are admin-only update at both the role-permission level (only ADMIN holds `nursing-note:update` / `progress-note:update` / `vital-sign:update` after V097) and the service layer (`*Service.updateXxx()` independently asserts ADMIN). Discharge protection blocks updates for everyone, including ADMIN. The `canEdit` field on `NursingNoteResponse`, `ProgressNoteResponse`, and `VitalSignResponse` evaluates to `isAdmin && admission.isActive`. Doctors, nurses, and chief nurses must request edits from an administrator. See `docs/features/nursing-module.md` revision 1.4 for the rationale on vital signs.
- Medical-order updates are not part of the default doctor, nurse, or chief-nurse roles. Doctors can create and discontinue orders, but only admin has the default update permission.
- Medical orders carry a category-driven workflow state. Directive categories (e.g. `DIETA`) start in `ACTIVA` and skip authorization. Auth-required categories (`MEDICAMENTOS`, `LABORATORIOS`, `REFERENCIAS_MEDICAS`, `PRUEBAS_PSICOMETRICAS`) start in `SOLICITADO`. Authorization (`SOLICITADO → AUTORIZADO`/`NO_AUTORIZADO`) is admin/administrative-staff only. Doctors can use emergency-authorize (`medical-order:emergency-authorize`) to self-authorize crisis or after-hours orders, with a structured reason captured in the audit trail. For results-bearing categories, nursing/admin/doctor can mark an `AUTORIZADO` order `EN_PROCESO` (sample taken / patient referred / test administered); after that, discontinue is no longer allowed. The `RESULTADOS_RECIBIDOS` terminal state cannot be set manually — it is reached only by uploading a result document, which auto-transitions the order in the same transaction (skipping `EN_PROCESO` if necessary). Discontinue (`DESCONTINUADO`) is allowed only from `ACTIVA`, `SOLICITADO`, or `AUTORIZADO`.
- Chief nurse is not just a label. In the shipped roles it extends nurse access with `patient:update` and `admission:update`. The legacy seed-file grant of `progress-note:update` to CHIEF_NURSE is revoked in V096; the V045 grant of `vital-sign:update` to CHIEF_NURSE / NURSE / DOCTOR is revoked in V097. CHIEF_NURSE now mirrors the per-record edit policy (admin-only update for nursing notes, progress notes, and vital signs).

## Planned Roles (Specs Drafted, Not Yet Shipped)

The following roles are specified but **not yet present in the codebase**. They appear here so reviewers can see where they will slot into the matrix once implemented; until the corresponding migrations land, no rows above grant them anything.

| Planned role | Spec | Summary |
| --- | --- | --- |
| `MAINTENANCE` | [features/warehouse-inventory-management.md](features/warehouse-inventory-management.md) | Operates one or more maintenance warehouses (assigned via `user_warehouses`). Holds `warehouse:read` (assigned only), `warehouse-transfer:create` (from assigned source), `warehouse-transfer:read`, `warehouse-charge:create`, `inventory-item:read`, `admission:read`, `patient:read`. Cannot read or write any clinical records. |

## Pending Permission Changes (Specs Drafted, Not Yet Shipped)

- **PSYCHOLOGIST gains scoped order-execution and scoped read** ([medical-psychiatric-record.md](features/medical-psychiatric-record.md) v1.6). The role is granted `medical-order:mark-in-progress` and `medical-order:upload-document`, but `MedicalOrderService` and `MedicalOrderDocumentService` will reject the calls with `error.medical-order.psychologist.category.scope` unless the order's category is `PRUEBAS_PSICOMETRICAS`. The role is also granted `medical-order:read` so the cross-admission dashboard endpoint (`GET /api/v1/medical-orders`) and the `/medical-orders` route — both gated by `medical-order:read` — are reachable; reads are service-layer scoped to `category = PRUEBAS_PSICOMETRICAS` (list filters those rows out for PSYCHOLOGIST; single-order fetches return 404 for non-psychometric categories). This codifies the customer's "psychologist can execute psychometric tests but no other orders" requirement.
- **Warehouse-scoped dispensing** ([warehouse-inventory-management.md](features/warehouse-inventory-management.md)). After V117/V118, every `medication-administration:create` and every `inventory-movement:EXIT` resolves a warehouse from the calling user (NURSE / CHIEF_NURSE / AUXILIARY_NURSE → `ENFERMERIA`, PSYCHOLOGIST → `PSICOLOGIA`, MAINTENANCE → `user_warehouses`, ADMIN / DOCTOR / RESIDENT_DOCTOR → explicit or default `ENFERMERIA`) and dispenses **only from that warehouse**. FEFO becomes warehouse-scoped. New error `error.warehouse.out-of-stock` is returned when the user's own warehouse is empty, even if another warehouse holds stock.

## Mismatches and Gaps To Track

- `USER` role mismatch: `V007__remove_user_read_from_user_role.sql` removes `user:read` from `USER`, but `R__seed_01_reset_and_base.sql` adds it back during reseed. In seeded environments the `USER` role can call the `/api/users` API, but the frontend still hides user administration behind `requiresAdmin` so it has no practical UI effect.
- `ADMINISTRATIVE_STAFF` billing mismatch: the seed file grants `billing:read` and `invoice:read`, but the versioned billing migration only grants billing read to doctor and nurse plus full billing/invoice access to admin.
- `ADMINISTRATIVE_STAFF` treasury mismatch: `V084__seed_treasury_permissions.sql` grants full treasury access to staff, but the reseed file rebuilds role permissions without replaying those treasury grants. In seeded dev or acceptance environments, treasury access can disappear after reseed.
- Document types are reachable by route and permission, but `web/src/layout/AppMenu.vue` does not expose a sidebar link for them.
- `CHIEF_NURSE` cannot upload medical-order documents despite being a nurse-plus role. `V073__add_medical_order_document_permissions.sql` grants `medical-order:upload-document` to `NURSE` but not `CHIEF_NURSE`.
- Psychotherapy activity create enforces the `PSYCHOLOGIST` role name at the service level (`PsychotherapyActivityService`), not just the `psychotherapy-activity:create` permission. A custom role with that permission but without the `PSYCHOLOGIST` role name would be rejected.
